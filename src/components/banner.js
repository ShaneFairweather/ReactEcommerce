import React from 'react';
import { Button } from 'react-bootstrap';

const Banner = () => {
    return (
        <div className="banner jumbotron">
            <Button className="bannerButton center">Start Shopping</Button>
        </div>
    )
}

export default Banner;